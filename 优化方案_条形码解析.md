# UDI 扫码 App — 条形码读取优化方案（待拍板）

> 基于当前代码（`Gs1Parser.kt` / `MainActivity.kt` / `NmpaClient.kt` / `ScanItem.kt`）梳理。
> 现状痛点：「扫出来的码有时候完全对不上」——根因是纯数字无 GS1 标记时判定偏宽松、以及变长 AI 在无 FNC1 时可能贪婪吞字段。

---

## 一、解析准确率（直接解决「对不上」）

### 1. 未知段智能归位（替代「一律待确认」）
- **现状**：纯数字非 GTIN、或 7–20 位纯数字 → 直接 `UNKNOWN`，需要用户手动挑。
- **优化**：结合上下文启发式归位：
  - 缓冲已有 UDI、且本段纯数字且不像日期 → 视为**批号**（多数标签把批号单独成码）。
  - 两位/三位「字母+数字」串（如 `LOT`、`A01`）→ 批号。
  - 纯数字 17~20 位且 UDI 已存在 → 序列号候选。
  - 仅当「UDI 都没有 + 单段纯数字」才保留 `UNKNOWN` 弹确认框。
- **收益**：绝大多数标签扫一次就好，不再弹确认打断流程。

### 2. 效期/生产日期年份歧义消除
- **现状**：`looksLikeDate` 只认 YYMMDD，YY<70 当 20xx、≥70 当 19xx；且 6 位段**一律判 EXPIRY**，导致生产日 `(11)` 被误标为效期。
- **优化**：
  - 区分 `(17)效期` 与 `(11)生产`：有 AI 标记时按标记；纯数字 6 位无标记时，**不再一律当效期**，改为「若缓冲已有 UDI 且前几秒无其他日期段 → 效期；否则待确认」。
  - 年份歧义：效期年份若换算后已过期超过 1 年（如 YY=20 但当前已 26），自动 +20 年修正并标红提示。
- **收益**：生产/效期不再混淆，错标率大幅下降。

### 3. 变长 AI 稳健切分（无 FNC1 时）
- **现状**：`parseStructured` 变长 AI 后用「遇到下一 AI 或末尾」切分——若后面紧跟非 AI 且无 FNC1，会一直吃到结尾，吞掉后续字段。
- **优化**：先对整个串做「AI 位置扫描」抽出所有 AI + 其起始偏移，再按位置切分，加软上限（批号 20、序列号 30）防止贪吃；缺失 FNC1 时降级为「按顺序 + 长度上限」切。
- **收益**：一码多字段且缺 FNC1 时不再错位。

### 4. 多码同框并行识别
- **现状**：`decodeContinuous` 默认一帧给一个结果，连续扫码靠「多次扫」合并多码。
- **优化**：开启多条形码模式（`BarcodeView` 的 multi 扫描或 `getRawBytes` 后手动聚类），一照片里 UDI 码 + 批号码并排时一次拿全字段；并在 UI 显示「本次识别到 X 个码」。
- **收益**：真正一次拍照拿全字段，减少分两次扫的错位风险。

### 5. GTIN 长度规范化
- **现状**：只认 14/13 位。
- **优化**：补 12→14、8→14 标准补位（`GTIN` 补零规则），提高 UPC/EAN 兼容。

---

## 二、扫码体验与防错

### 6. 误读防抖 / 重复去抖增强
- **现状**：`scannedRaws` 仅在「本次缓冲」去重；同帧狂触发会反复嘀。
- **优化**：加 800ms 时间窗去抖（`lastBeepTime`）+ 跨缓冲「已入库 UDI」集合，避免上一个 SKU 没清空时重复入库。

### 7. 震动反馈 + 可关闭的「滴」声
- **现状**：`beep()` 固定播放系统通知音，个别机型无声也无震动。
- **优化**：同时 `Vibrator` 短震（15~30ms）；设置里加「音效 / 震动」开关（默认开）。

### 8. 缓冲冲突告警
- **现状**：已存在 UDI 又扫到不同 UDI → 静默「已存在则保留」。
- **优化**：检测到新 UDI 与已缓冲 UDI 不一致时，闪烁黄条提示「检测到新 UDI，是否开新条目？」，避免把不同货品混进同一条。

### 9. 离线降级与失败重试
- **现状**：`NmpaClient` 失败直接 `err`，无重试。
- **优化**：失败自动重试 1 次；仍失败保留 `err` 但允许「点产品区手动补全」，并记入待同步队列（配合自动化下次联网补查）。

---

## 三、数据质量与可追溯

### 10. 保留原始码 + 识别置信度
- 在 `Gs1Result` 增加 `confidence` / 字段来源（`括号` / `FNC1校验` / `启发式`），导出时附 `raw_barcode`、`parse_note`，便于桌面端审计「为什么这条批号是推断的」。

### 11. 重复 UDI 校验
- 加入清单前，若该 UDI 已在本次清单出现 → 高亮「重复」并默认合并数量而非新增，避免同一盒扫两次。

### 12. 扫码审计日志
- 落一张本地 `scan_log` 表（时间戳、原始串、解析结果、是否人工修正），供「为什么这条错了」回溯，也方便后续训练判定阈值。

---

## 四、UI / 交互

### 13. 实时「本次将录入」预览卡
- 缓冲区上方显示「将成为一条：UDI × 批号 × 效期 × 数量」卡片，扫到即更新，提交前一眼确认。

### 14. 字段手动指定入口
- 对 UNKNOWN 段，长按弹出「这是什么：UDI/批号/效期/生产/序列」快捷选择（你之前提过的「手动指定字段」）。

### 15. 取景框辅助引导
- 取景框叠加「请将条码对准框内」引导线，提升一次命中率（弱光/曲面包装常见失败）。

---

## 五、工程与扩展

### 16. 解析单测化
- 把 `Gs1Parser` 抽出纯函数层，加 JVM 单测（括号、FNC1、纯数字、12/8 位、误切样例），防止后续改判定规则回归。

### 17. OCR 兜底（拍照识别）
- 对撕码、反光、曲面导致扫不出的标签，拍照 → OCR 提取数字 → 喂给解析器，与现有 `medical-inventory-ocr` 能力打通。

---

## 建议落地顺序（可由你调整）
**一期（直接解决「对不上」+ 体验闭环）**：1、2、3、6、7、13、14
**二期（数据质量与扩展）**：4、5、8、9、10、11、12
**三期（工程与兜底）**：15、16、17

---
请勾选要实施的项（可增减、可调顺序）。确认后我进入 Craft 模式逐条落地。
