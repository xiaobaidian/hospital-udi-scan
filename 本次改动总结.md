# 本次改动总结（2026-08-24）

## 一、条形码解析引擎重写（核心，解决「扫出来对不上」）

### 问题根因
旧 `Gs1Parser` 假设扫码串带括号 `(01)` 或 FNC1 分隔符。但实测 zxing 在多机型把 FNC1 剥离，
扫到的串是 `01` + 14位UDI + `10` + 批号 这种**无括号、纯前缀拼接**。旧逻辑检测不到 FNC1 →
误入「纯数字判定」→ 多字段串被当成一个 GTIN + 垃圾，批号/效期全丢。

### 新引擎：前缀扫描 + 位置切分
- **归一化**：剥括号；FNC1 直接删除（不留空格，避免定长值误吞分隔符）。
- **不以核心 AI 开头 → 兜底**：裸日期 `250631`、裸非 GTIN 14 位等直接走启发式，不切碎。
- **主路径**：扫整串所有 AI 前缀位置，按定长/变长切值。
  - AI 集合精简为 `01/02/11/17`(定长) + `10/21/91`(变长)，移除易冲突的 `250/12/13/15/16/20/...`。
  - 变长值边界 = 下一真 AI 起点；定长 AI 边界需满足后续长度（防 `2024` 里的 `02` 误判）。
- **兜底**：GTIN-14/13/12/8 校验位判定、6位像日期→效期、歧义→UNKNOWN 待指定。
- **验证**：`tools/verify_parser.py`（Python 复刻，12 真实样例全过）+ `app/src/test/.../Gs1ParserTest.kt`（随 CI 回归）。

## 二、UI 交互改造（你拍板先做的部分 + 震动/冲突）

1. **实时预览卡（更醒目）**：缓冲卡上方独立高亮蓝卡，四个核心字段 chip + 数量 + 待确认提示，扫到即刷新。
2. **字段手动指定入口**：待确认段显橙色可点击 chip，点开弹「UDI/批号/效期/生产/序列」选择。
3. **取景框引导**：绿色虚线描边框 + 四角提示，提升一次命中率。
4. **去抖**：800ms 时间窗 + 同串去重，防连帧狂触发/重复入库。
5. **震动 + 音效**：扫码成功系统「滴」声 + 短震 30ms。
6. **UDI 冲突告警**：已缓冲 UDI 又扫到不同 UDI → 自动提交当前条目并开新条目 + 黄条提示。

## 三、构建出包
- 推送：GitHub REST API (Git Data) → commit `df6a19d169` → 触发 Actions 构建。
- APK 直链：`https://github.com/xiaobaidian/hospital-udi-scan/releases/download/debug-latest/app-debug.apk`
- 状态：CI `32682630194` 构建中，完成后验证下载。

## 四、下次实测重点
1. 「滴」声 + 震动是否生效。
2. 预览卡字段是否正确归位（尤其「纯前缀拼接」UDI+批号+效期一次扫全）。
3. 手动指定 chip 是否好用。
4. 若仍有具体条码误判，发样例 → 调 `fallbackHeuristic` 阈值或扩 AI 集合。
